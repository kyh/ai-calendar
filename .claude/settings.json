{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "prompt": "AI-native calendar template built to be driven by coding agents — read AGENTS.md first. Provisioning is `pnpm install && pnpm dev`; there is no database, no auth and no bootstrap script. The calendar is fully verifiable without any config; only the chat loop needs AI_GATEWAY_API_KEY in .env.local. Never run `eve build` while `pnpm dev` is running.",
  "permissions": {
    "allow": [
      "Bash(pnpm verify:*)",
      "Bash(pnpm typecheck:*)",
      "Bash(pnpm lint:*)",
      "Bash(pnpm format:fix:*)",
      "Bash(pnpm build:*)",
      "Bash(agent-browser:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$CLAUDE_CODE_REMOTE\" = \"true\" ] && cd \"$CLAUDE_PROJECT_DIR\" && pnpm install || true",
            "timeout": 600
          }
        ]
      }
    ]
  }
}
